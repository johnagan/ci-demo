{
  "name": "github-ci-demo",
  "version": "0.0.1",
  "description": "Sample",
  "author": "John Agan <johnagan@github.com>",
  "scripts": {
    "test": "mocha -c --compilers coffee:coffee-script/register",
    "deploy": "git subtree push --prefix dist https://$GH_TOKEN@$GH_REF gh-pages"
  },
  "devDependencies": {
    "coffee-script": "~1.9.1",
    "mocha": "~2.2.1"
  }
}
