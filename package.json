{
  "name": "github-ci-demo",
  "version": "0.0.1",
  "description": "Sample",
  "author": "John Agan <johnagan@github.com>",
  "scripts": {
    "test": "mocha -c --compilers coffee:coffee-script/register",
    "deploy": "git subtree push --force --quiet --prefix dist https://$GH_TOKEN@$GH_REF master:gh-pages > /dev/null 2>&1"
  },
  "devDependencies": {
    "coffee-script": "~1.9.1",
    "mocha": "~2.2.1"
  }
}
